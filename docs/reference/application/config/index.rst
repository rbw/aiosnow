Config
======

The :class:`snow.Application` takes a config dictionary in its constructor. Once validated and transformed, the result is assigned to :attr:`snow.Application.config`.

<input/output config table>

**Contents**

.. toctree::

    core
    secrets
